{
  "name": "spoo-visual-tests",
  "private": true,
  "version": "1.0.0",
  "description": "SPOO 사이트 시각적 회귀 테스트 (Playwright)",
  "scripts": {
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots",
    "test:visual:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
