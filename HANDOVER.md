# SPOO 운영 인수인계 문서 (HANDOVER)

> 이 저장소를 관리하는 사람이 알아야 할 것들을 모아둔 문서입니다.
> CI 워크플로(`repo-checks.yml`)와 TESTING.md가 이 문서를 참조합니다.
> 원래 있던 HANDOVER.md가 유실되어 2026-08-28 점검 때 다시 작성했습니다.

---

## 1. 이 저장소의 구조 한눈에

- 루트의 `{시군구코드}.json` 229개 = 지역별 시설 데이터. **GitHub Pages로 그대로 서빙**되므로 루트에 올린 모든 파일은 인터넷에 공개됩니다.
- `app.js` / `index.html` / `style.css` = 사이트 전부 (빌드 과정 없음).
- `config.json` = **매년 바뀌는 값은 전부 여기만** 고치면 됨 (신청기간·결제마감·월 지원 한도·데이터 기준일). 코드 안의 숫자를 직접 찾을 필요 없음.
- `i18n/` = 실제 사용되는 번역 파일. **루트에 en.json 등을 두면 안 됨** (5장 참고).
- `tools/check_data_quality.py` = 데이터 품질 게이트. push마다 CI에서 자동 실행.

## 2. 저장소에 있으면 안 되는 파일 (CI가 자동 감시)

| 파일 | 이유 |
|---|---|
| `voucher_facilities.csv` | 3.7MB 원본 덤프. 지역별 JSON으로 쪼개 대체됐음 — 다시 올리면 두 벌 관리가 됨 |
| `facilities.zip`, `*.zip`, `*.rar` | 압축 파일은 Pages로 공개되고, 사이트의 옛 사본이라 실제 코드와 어긋남. 실제로 2026-08에 옛 사본 zip이 공개 배포된 사고 있었음 |
| `01_initial.png`, `04_region_view.png`, `README (1).md`, `gitignore`(점 없는), `면허` | 과거 실수로 올라왔던 잡파일들 |
| 루트의 `en.json` `vi.json` `zh.json` | 앱은 `i18n/` 쪽만 읽음. 루트 사본은 "고쳤는데 반영이 안 되는" 혼란만 만듦 |

## 3. 실제로 반복됐던 실수들 (꼭 읽기)

1. **웹 업로드 시 폴더 구조가 풀림.** GitHub 웹에서 파일을 드래그해 올리면
   `tools/`나 `.github/workflows/` 안으로 가야 할 파일이 루트에 떨어지기 쉽다.
   → 예방법: **목적지 폴더에 먼저 들어간 뒤** `Add file → Upload files` 하거나,
   폴더 페이지 주소(`github.com/1597circle/SPOO/tree/main/tools`)로 직접 이동해서 올릴 것.
2. **i18n 이중 사본.** 번역을 고쳤는데 화면이 안 바뀌면, 루트의 죽은 사본을 고친 것.
   실제 파일은 `i18n/` 안에 있다. (품질 게이트가 루트 사본 재등장을 감시함)
3. **zip 커밋.** `.gitignore`에 `*.zip`이 있어도 **이미 추적 중인 파일은 못 막는다.**
   지울 땐 `git rm --cached` 또는 웹에서 Delete file.
4. **테스트의 `page.goto('/')` 함정.** `'./'`를 써야 함 — `'/'`는 `/SPOO/` 경로를 지워
   404 페이지를 테스트하게 됨 (실제 사고 있었음).
5. **버전 문자열 3곳.** 배포 시 함께 확인:
   `index.html`의 `?v=YYYYMMDD` 쿼리(app.js·style.css·naver-auth-handler),
   `service-worker.js`의 `CACHE_NAME`(spoo-vN),
   `config.json`의 `dataUpdatedAt`.

## 4. 매년 데이터 갱신 절차

1. 새 시즌 데이터(시설·강좌·수급 통계)를 받아 같은 형식으로 교체.
2. `config.json` 값 갱신 — `applyPeriod`(+ 공고 확정 시 `applyPeriodConfirmed: true`),
   `paymentDeadline`, `voucherMonthlyLimit`, `dataUpdatedAt`.
3. 로컬 또는 CI에서 `python3 tools/check_data_quality.py` 실행.
   - 임계값(BASELINE)은 "현재 수치" 기준이므로, 품질을 **개선**했다면 스크립트 상단
     BASELINE 숫자도 같이 올릴 것 (예: 지오코딩 후 좌표 커버리지 하한).
4. 화면이 의도적으로 바뀌었으면 Actions의 "기준 스크린샷 생성" 워크플로 실행.

## 5. 자동 검사 3종 요약

| 워크플로 | 언제 | 실패하면 |
|---|---|---|
| 저장소 점검 (repo-checks.yml) | push·PR마다 | 에러 메시지에 파일명·이유가 한국어로 나옴. 대부분 금지 파일/JSON 깨짐/데이터 정합성 |
| 시각적 회귀 테스트 | 매일 06:00 KST + push | ① 데이터 로딩 어서션 실패 = 사이트가 실제로 죽은 것 → 최우선 확인 ② 스크린샷 차이 = 의도한 변경이면 기준 스크린샷 갱신 |
| 기준 스크린샷 생성 | 수동 (Run workflow) | 디자인 변경 후 한 번 실행 |

## 6. 알려진 미해결 사항 (2026-08-28 기준)

- **인천 행정구역 개편(2026-07-01) 데이터 이관.** 신설 4개 구(제물포구·영종구·서해구·검단구)는
  현재 검색 별칭(`app.js`의 `REGION_ALIASES`)으로만 대응 중. 통계·경계·시설 데이터가
  신설 구 코드 기준으로 재공급되면 완전 이관할 것. **주소만으로 서해구/검단구를 나누지 말 것**
  (경계가 경인아라뱃길이라 오분류 위험이 큼).
- **시설 좌표 결측.** `tools/geocode_missing.py` + "좌표 채우기" 워크플로로 보충 가능
  (네이버 지오코딩 API 키 필요 — 워크플로 파일 안 주석 참고).
- **강좌–시설 연결 키에 시도(sido) 없음.** 대도시 동명 구(중구 6곳 등)에서 동명 시설이
  충돌하는 사례 24쌍(강좌 106행). courses.csv에 sido 컬럼이 추가되는 시점에 키를
  `sido|sgg|name`으로 바꿀 것.
- **"도움이 필요한 지역 TOP 10" 기준.** 현재 `computeTop10Priority()`가
  두 집단 수급률 순위 평균으로 계산. 다른 기준을 원하면 그 함수의 정렬만 바꾸면 됨.

## 7. 점검·수정 이력

- **2026-08-28 전체 점검** — 데이터 무결성·코드·운영체계 점검(발견 26건).
  주요 수정: ① 지도 장애와 데이터 로딩 분리(지도가 죽어도 사이트 동작)
  ② 인천 신설 구 검색 별칭 ③ TOP 10 하드코딩 → 데이터 계산 전환
  ④ 지원 한도 config.json 이전 ⑤ 데이터 품질 게이트·데이터 로딩 어서션 신설
  ⑥ CSV 파서 여러 줄 필드 버그 수정(60,533행 정확 파싱) ⑦ HTML 엔티티 디코딩
  ⑧ 전화번호 검증 ⑨ 서비스워커 캐시 안전화 ⑩ theme-color 통일.
