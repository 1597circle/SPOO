# 시각적 회귀 테스트 & CI 사용 안내

이 문서는 `.github/workflows/`와 `tests/`에 새로 추가된 자동화 설정을 어떻게 쓰는지 정리한 것입니다.
(참고: HANDOVER.md에는 `visual_test.js`가 "완료"로 적혀 있었지만 실제 저장소엔 없어서, 이번에
Playwright 기반으로 새로 만들었습니다.)

---

## 1. 뭐가 자동으로 돌아가나요?

| 워크플로 | 언제 실행되나 | 뭘 하나 |
|---|---|---|
| `repo-checks.yml` | 모든 push·PR | 삭제됐어야 할 파일 재업로드 감지, JSON 깨짐 감지, 229개 지역 데이터·인접 매핑 정합성 검증, **데이터 품질 게이트**(아래 참고) |
| `visual-regression.yml` | 매일 06:00(KST) 자동 + main push 시 + 수동 실행 | 실제 배포 사이트 스크린샷을 찍어 기준 이미지와 비교 |

두 워크플로 모두 **GitHub 저장소에 `.github/workflows/` 폴더를 올리기만 하면** 별도 설정 없이 바로 작동합니다 (Actions 탭에서 확인 가능).

---

## 2. 처음 한 번 해야 하는 것: 기준(baseline) 스크린샷 만들기

시각 테스트는 "기준 이미지"가 없으면 비교할 대상이 없어서 처음엔 실패합니다. 로컬 컴퓨터에서 한 번만 아래를 실행해주세요.

```bash
npm install
npx playwright install --with-deps chromium
npm run test:visual:update
```

실행하고 나면 `tests/visual.spec.js-snapshots/` 폴더에 `.png` 파일들이 생깁니다. **이 폴더를 반드시 GitHub에 함께 커밋(업로드)**해야 이후 CI에서 비교할 수 있습니다.

---

## 3. 평소 사용법

- **로컬에서 비교만 해보고 싶을 때**: `npm run test:visual`
- **디자인을 의도적으로 바꿔서 새 기준이 필요할 때**: `npm run test:visual:update` 후 바뀐 스냅샷 파일들을 다시 커밋
- **실패 원인을 이미지로 보고 싶을 때**: 테스트 실행 후 `npx playwright show-report` (diff 이미지가 나란히 표시됨)
- **GitHub Actions에서 실패했을 때**: Actions 탭 → 해당 실행 → `playwright-report` 아티팩트 다운로드 → 압축 풀고 `index.html` 열기

---

## 4. 지금 커버하는 화면 / 안 하는 것

**커버함**
- **데이터 로딩 검증** — 229개 지역 수급 데이터·시설 개수·검색 목록·지역 경계가 실제로 들어왔는지
- **지도 장애 격리 검증** — 네이버 지도를 차단해도 나머지 기능이 살아있는지
- 첫 화면(자가진단 진입 전)
- 지역 현황 보기(5-2) 진입 화면
- 시설 운영자(5-3) 진입 화면
- 테마 색상(`#3182F6`)·타이틀 유지 여부

> ⚠ 스크린샷 비교만으로는 "사이트가 죽은 것"을 못 잡습니다. 첫 화면과 진입 화면은 정적 HTML이라,
> 데이터가 하나도 안 들어와도 픽셀이 똑같아서 테스트가 통과해버립니다.
> 그래서 위의 **데이터 로딩 검증**을 각 화면 테스트 앞에 함께 두었습니다.

**아직 안 함 (다음에 추가하면 좋은 것)**
- 지역 현황 보기에서 강남구 검색 후 상세 화면 (이전 문서엔 "커버함"으로 적혀 있었으나 실제 코드엔 없었습니다)
- 자가진단 전체 플로우(나이·가구유형 선택 후 결과 화면)
- 지도(네이버맵)가 보이는 상태 — 외부 스크립트 로딩 타이밍 때문에 스크린샷이 불안정해서 일부러 뺐습니다
- 5-2의 ①~⑦ 세부 화면 각각, 5-3의 ②~⑤ 세부 화면
- 모바일 온보딩 플로우(이름 입력부터 시작하는 첫 방문자 분기)

`tests/visual.spec.js`에 같은 패턴으로 `test(...)` 블록을 추가하면 됩니다. 화면 전환은 대부분
`window.함수이름()` 형태로 직접 호출 가능하니, `app.js`에서 `function open...`, `function toggle...`
로 검색해서 필요한 함수를 찾아 쓰면 됩니다.

---

## 5. CI가 실패했을 때 흔한 원인

- **`repo-checks.yml` 실패** → 대부분 "금지 파일이 다시 올라옴" 또는 "JSON이 깨짐" 둘 중 하나입니다. 에러 메시지에 파일명이 정확히 나오니 그것만 보면 됩니다.
- **`visual-regression.yml` 실패** → 진짜 레이아웃이 깨졌거나, 배포 직후라 아직 캐시가 안 갱신됐거나(재실행해보기), 또는 의도적인 디자인 변경이라 기준 이미지 갱신이 필요한 경우입니다.


---

## 6. 데이터 품질 게이트

`tools/check_data_quality.py`는 "파일이 있는지 / JSON이 깨졌는지"만으로는 안 잡히는 것들을 봅니다.

```bash
python3 tools/check_data_quality.py     # 로컬에서도 그냥 실행됩니다
```

검사 항목:

| 검사 | 실패 조건 |
|---|---|
| 코드 집합 정합성 | 5개 데이터 소스의 229개 지역 코드가 하나라도 어긋남 |
| 시설 개수 캐시 | `facility_counts.json`이 실제 레코드 수와 다름 |
| 수급률 파생값 | `s_pct`/`n_pct`를 재계산한 값과 0.05%p 이상 차이 |
| 인접 시군구 매핑 | 존재하지 않는 이웃 코드, 비대칭 관계, 자기 참조 |
| 시설 좌표 | 커버리지가 기준선(51.0%) 아래로 떨어지거나 한반도 밖 좌표 발생 |
| 강좌 데이터 | 시설 조인율 97% 미만, 중복 급증, 시간 이상치 7% 초과 |
| 다국어 파일 | 루트에 `en.json` 등이 다시 생김 (앱은 `i18n/`만 읽습니다) |
| config.json | 신청·결제 마감일이 이미 지났거나 금액 기준값이 빠짐 |
| 저장소 위생 | `.zip`/`.rar`이 커밋됨 |

**임계값은 "완벽함"이 아니라 "지금 수치"가 기준입니다.** 악화될 때만 실패하므로,
품질을 개선했다면 스크립트 상단의 `BASELINE` 숫자를 함께 올려주세요.

경고(`::warning::`)는 빌드를 실패시키지 않습니다. 지금 알려진 채로 추적 중인 항목
(좌표 없는 26개 지역, 인천 행정구역 이관, `HANDOVER.md` 부재 등)이 여기에 나옵니다.
