# SPOO 디자인 토큰

`style.css`의 `:root{}` 안에 정의된 CSS 변수들을 문서로 정리했어요.
**색이나 글자 크기를 새로 추가하거나 바꿀 땐, 값을 직접 쓰지 말고 이 문서에서 맞는 변수를 찾아 `var(--이름)`으로 써주세요.**
오늘 색상 통일 작업 때 하드코딩된 값들을 찾느라 여러 번 반복했던 게, 바로 이 원칙이 안 지켜져서였어요.

## 1. 색상 팔레트

### 브랜드 강조색

| 변수 | 값 | 역할 |
|---|---|---|
| `--navy` | `#181C22` (거의 검정) | 제목·숫자 등 강조 텍스트 색 (배경 아님!) |
| `--navy-gradient` | `linear-gradient(135deg, #4C8DFF, #1B4FD6)` | **주요 버튼·헤더·배너 배경** — 시작하기, 다음 단계, 지역현황 카드 등 |
| `--navy-soft` | `#154FAD` | (현재 사용처 없음 — hover는 `filter:brightness(1.08)` 방식으로 통일함) |
| `--blue` | `#3182F6` | 링크, 포커스 테두리 |
| `--blue-soft` | `#EAF2FF` | 연한 파란 배경 (비교 버튼 등 보조 버튼) |
| `--blue-deep` | `#1B64DA` | 진한 파란 텍스트/테두리, PWA theme-color |
| `--mint` / `--mint-soft` | `#0AC5A8` / `#E3F9F5` | 기초생활수급(S그룹) 지표 강조, "양호" 상태, 긍정적 결과 |
| `--red` / `--red-soft` | `#F04452` / `#FDECEE` | 실제 오류·경고 메시지 (진짜 "위험"을 뜻할 때만) |
| `--amber` | `#F5A623` | (현재 사용처 없음) |

### ⚠️ 알려진 기술 부채 — 언젠가 정리 필요

`--coral`, `--coral-soft`, `--coral-deep`은 지금 **`--blue-deep`, `--blue-soft`와 완전히 똑같은 값**이에요.
원래는 주황색이었는데(`#F0603F`), 파란색 통일 작업 때 값만 바꿔서 임시로 맞춰놓은 상태라 이름과 실제 색이 안 맞아요.
나중에 여유 있을 때 `--coral` 계열 변수를 지우고 그 자리를 전부 `--blue-deep`/`--blue-soft`로 바꾸는 걸 추천해요 (지금은 위험 부담 때문에 남겨둠).

### 중립색 (배경·텍스트·선)

| 변수 | 값 | 역할 |
|---|---|---|
| `--paper` | `#F7F5F1` | 페이지 배경 |
| `--gray-bg` | `#F1EFE9` | 카드 안 옅은 배경 (통계 박스 등) |
| `--ink` | `#181C22` | 기본 본문 텍스트 |
| `--ink-sub` | `#525A66` | 보조 설명 텍스트 |
| `--ink-faint` | `#6B7280` | 캡션·placeholder |
| `--line` | `#E7E3DA` | 테두리 |
| `--card` | `#FFFFFF` | 카드 배경 |

### 그림자

| 변수 | 역할 |
|---|---|
| `--shadow-sm` | 버튼 등 가벼운 입체감 |
| `--shadow-md` | hover 시 살짝 떠오르는 느낌 |
| `--shadow-lg` | 배너·모달처럼 화면 위에 뜬 요소 |

## 2. 타이포그래피 스케일

화면마다 12.5~16.5px 사이 값이 제각각 쓰이던 걸 6단계로 정리했어요.
**숫자 크기가 아니라 "이 텍스트가 어떤 역할인가"로 골라 쓸 것.**

| 변수 | 크기 | 용도 |
|---|---|---|
| `--fs-display` | `clamp(34px, 6.2vw, 52px)` | 온보딩·자가진단 전체화면 질문 |
| `--fs-h1` | `clamp(21px, 2.6vw, 25px)` | 화면 대표 제목 |
| `--fs-h2` | `16px` | 카드 안 소제목 |
| `--fs-body` | `15px` | 본문 기본 |
| `--fs-label` | `13.5px` | 보조 라벨·설명 |
| `--fs-caption` | `12.5px` | 캡션·힌트 |

## 3. 버튼 hover 규칙

그라데이션 배경 버튼(`background:var(--navy-gradient)`)의 hover는 **절대 배경색을 다른 색으로 바꾸지 마세요.**
그라데이션이 단색으로 순간 바뀌어서 어색해 보여요. 대신 이렇게 쓰세요:

```css
.내버튼:hover{ filter:brightness(1.08); transform:translateY(-1px); }
```

## 4. 새 색을 추가하기 전에 체크리스트

- [ ] 이미 있는 변수로 표현 안 되는 색인가? (거의 대부분 이미 있음)
- [ ] `:root{}`에 변수로 추가했는가? (파일 안에 직접 hex 값 쓰지 않기)
- [ ] box-shadow의 `rgba(...)` 색상도 브랜드 색과 맞는 톤인가? (예전에 배경은 파랑인데 그림자만 주황인 버그가 있었음)
- [ ] hover 상태가 그라데이션을 깨뜨리지 않는가?
